import React, { useState } from 'react';

const NavBar = () => {
  return (
    <nav className="p-4 flex items-center justify-end">
      <h1 className='text-white font-semibold text-xl'>Fraud Detection Dashboard</h1>
    </nav>
  );
};

export default NavBar;
